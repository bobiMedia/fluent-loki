<match live4-auth-service.dev.DEBUG>
  @type loki
  url "#{ENV['LOKI_URL']}"
  extra_labels {"env":"dev", "app":"live4-auth-service", "level": "debug"}
  <buffer>
      flush_interval 10s
      flush_at_shutdown true
  </buffer>
</match>
<match live4-auth-service.dev.INFO>
  @type loki
  url "#{ENV['LOKI_URL']}"
  extra_labels {"env":"dev", "app":"live4-auth-service", "level": "info"}
  <buffer>
      flush_interval 10s
      flush_at_shutdown true
  </buffer>
</match>
<match live4-auth-service.dev.WARN>
  @type loki
  url "#{ENV['LOKI_URL']}"
  extra_labels {"env":"dev", "app":"live4-auth-service", "level": "warn"}
  <buffer>
      flush_interval 10s
      flush_at_shutdown true
  </buffer>
</match>
<match live4-auth-service.dev.ERROR>
  @type loki
  url "#{ENV['LOKI_URL']}"
  extra_labels {"env":"dev", "app":"live4-auth-service", "level": "error"}
  <buffer>
      flush_interval 10s
      flush_at_shutdown true
  </buffer>
</match>
<match live4-auth-service.dev.FATAL>
  @type loki
  url "#{ENV['LOKI_URL']}"
  extra_labels {"env":"dev", "app":"live4-auth-service", "level": "fatal"}
  <buffer>
      flush_interval 10s
      flush_at_shutdown true
  </buffer>
</match>
<match live4-auth-service.dev.**>
  @type loki
  url "#{ENV['LOKI_URL']}"
  extra_labels {"env":"dev", "app":"live4-auth-service", "level": "unknown"}
  <buffer>
      flush_interval 10s
      flush_at_shutdown true
  </buffer>
</match>